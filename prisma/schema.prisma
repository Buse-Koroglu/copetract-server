generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Notification {
  id Int @id @default(autoincrement())
  date DateTime @default(now())
  title String 
  message String
  category String @default("Status")
  isRead Boolean @default(false)
}